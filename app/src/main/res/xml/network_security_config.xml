<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext for local dev (e.g. http://192.168.x.x:5173). Remove in strict production if not needed. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
